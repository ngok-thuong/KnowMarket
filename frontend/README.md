# Frontend (FE)

## Purpose
- Next.js UI: feed, post page (free/premium preview), purchase flow, decrypt render, contributions UI.
- Wallet connect + signature login.

## Suggested structure
- `app/` or `src/`: UI routes/components
- `public/`: static assets

